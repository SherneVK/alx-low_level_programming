#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: The first number to muliply
 * @y: The second number to muliply with the first number
 * Return: The result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
